// firebase.js — Firebase Configuration & Initialization
//
// Sets up Firebase app using env vars (VITE_FIREBASE_*).
// Exports: Firebase app instance, Firestore db, and Auth.
// Used for Google sign-in and storing session data (post-MVP).
